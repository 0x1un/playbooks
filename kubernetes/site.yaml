---

# - hosts: kube-cluster
#   gather_facts: yes
#   roles:
#     - { role: dockerce }

- hosts: master
  gather_facts: yes
  roles:
    - { role: dockerce }
    - { role: k8s/master }
    - { role: cni }
  
  # tasks:
  #   - name: test
  #     debug:
  #       msg: "{{ master_ip }}"